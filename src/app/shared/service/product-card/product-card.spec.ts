import { TestBed } from '@angular/core/testing';

import { Service } from './product-card';

describe('ProductCard', () => {
  let service: Service;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(Service);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
